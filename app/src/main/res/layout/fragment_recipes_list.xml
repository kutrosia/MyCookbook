<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="100dp">

        <EditText
            android:id="@+id/search_edit_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <Button
            android:id="@+id/search_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/search50"
            android:layout_alignParentEnd="true"/>

        <Button
            android:id="@+id/category_filter_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/category_filled50"
            android:layout_below="@+id/search_edit_text"
            />

        <Button
            android:id="@+id/name_filter_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/restaurant_menu50"
            android:layout_below="@+id/search_edit_text"
            android:layout_centerHorizontal="true"
            />

        <Button
            android:id="@+id/time_filter_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/sand_timer_filled50"
            android:layout_below="@+id/search_edit_text"
            android:layout_alignParentEnd="true"
            />

    </RelativeLayout>

    <ListView
        android:id="@+id/list_view_item"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        />

</LinearLayout>